# 지켜야 할 디자인 원칙

지난 세션에서 이 대시보드를 다듬으며 정리해둔 원칙입니다. 새 화면을 붙일 때 그대로 적용합니다.

| 원칙 | 내용 |
| --- | --- |
| 색을 새로 고르지 않는다 | `COLOR_*` 상수만 사용 (임의로 헥스코드를 만들지 않음) |
| 어색한 분할보다 단순한 전체 폭 | 억지로 좌우 2단 나누지 않고, 세로로 쌓는 흐름 |
| 같은 종류 카드는 완전히 균등하게 | KPI 카드 3개는 크기·정렬을 정확히 동일하게 |
| 형식적 통일보다 내용적 정확성 | 차트 제목은 실제 내용을 정확히 반영 (억지로 다른 차트와 맞추지 않음) |
| 게이지(`go.Indicator`)를 쓴다면 `title_font` 제외 | `CHART_LAYOUT`을 그대로 스프레드하면 빈 title 객체가 생겨 "undefined"가 화면에 그대로 렌더링됨 |
